# API base URL — default is /api/v1 which the vite proxy forwards to VITE_BACKEND_URL
VITE_API_BASE_URL=/api/v1

# Backend origin for Vite dev proxy (only used in local dev)
VITE_BACKEND_URL=http://localhost:8460
